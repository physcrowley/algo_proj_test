import java.io.*;
import java.util.*;

public class Driver {

    /**
     * Pour tester manuellement vos algorithmes avec le débogueur si les
     * tests unitaires dans AlgosTest échouent.
     * 
     * Les gabarits ci-dessous n'affichent rien parce que l'intention est
     * des consulter les informations disponibles dans le débogueur durant
     * l'exécution du programme.
     */
    public static void main(String[] args) {
        Algos testClass = new Algos();

        // gabarit de test pour willNotCrashLoadData()
        // testClass.willNotCrashLoadData(new File("./test_data/empty.qz"));

        // gabarit de test pour fileStructure()
        // testClass.fileStructure(new File("./test_subfolderA/"));

        // gabarit de test pour prettyFileStructure()
        // testClass.prettyFileStructure(
        //         "test_subfolderA(a(b(\"xx.txt\",\"yy.txt\",),\"x.txt\",),c(\"z.txt\",\"zzz.txt\",),),");
    }

}
